edit the app/config/config.yml with your database settings

create a /cache folder with permissions for the Web server process to create files

dbscript will create the database tables as they are needed

and you are done!

voila
